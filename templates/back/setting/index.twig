{% extends "back/base.html.twig" %}

{% block title %}Les paramètres{% endblock %}

{% block body %}

    <h1>Les paramètres</h1>

    {{ form_start(form) }}
    
        <div class="form-group">
            {{ form_label(form.title) }}
            {{ form_widget(form.title) }}
        </div>

        <div class="form-group">
            {{ form_label(form.url) }}
            {{ form_widget(form.url) }}
        </div>

        <div class="form-group">
            {{ form_label(form.email) }}
            {{ form_widget(form.email) }}
        </div>

        <div class="form-group">
            {{ form_label(form.home) }}
            {{ form_widget(form.home) }}
        </div>

        <div class="form-group">
            {{ form_label(form.google_analytics_code) }}
            {{ form_widget(form.google_analytics_code) }}
        </div>

        {{ form_row(form.send) }}

    {{ form_end(form) }}

{% endblock %}