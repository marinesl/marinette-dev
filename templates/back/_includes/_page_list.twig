<div>
    <table id="table" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nom</th>
                <th>Statut</th>
                <th>Modifié le</th>
            </tr>
        </thead>

        <tbody>
            {% for page in pages %}
                <tr data-slug="{{ page.slug }}">
                    <td>{{ page.id }}</td>
                    <td>{{ page.title }}</td>
                    <td>{{ page.status.name }}</td>
                    <td>{{ page.editedAt|date('d/m/Y H:i') }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>